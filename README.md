# arquitecturabase-psw

Comando para el despliegue de la arquitectura base para proyectos de Programación de Software (PSW).
``gcloud run deploy arquitecturabase-psw --source . --region=europe-west1 --env-vars-file=env.yml --allow-unauthenticatedexit
``